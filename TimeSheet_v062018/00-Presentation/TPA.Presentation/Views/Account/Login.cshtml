@using TPA.Presentation.Models
@model LoginViewModel
@{
    ViewBag.Title = "Efetuar login";
}

<h2>@ViewBag.Title.</h2>
<div class="row">
    <div class="col-md-8">
        <section id="loginForm">
            @using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
            {

                <h4>Usar uma conta local para efetuar login.</h4>
                <p>
                    A tela de login da Microsoft pode ser bloqueada por proxy ou firewall, por isso adicionamos a possibilidade de uma senha local. 
                    Se isso acontece no seu estabelecimento, acesse o sistema uma primeira vez de sua casa ou smartphone, efetue o login clicando no botão "Tecnun" e logo depois clique no seu e-mail, no canto superior direito, para editar suas informações pessoais.
                    Nessa página você pode adicionar uma senha interna e informações pessoais. Uma vez definida a senha interna, você pode sempre entrar com ela, mesmo em ambientes onde o login da Microsoft é bloqueado.       
                </p>
                <hr />
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                <div class="form-group">
                    
                    <div class="col-md-10">
                        <div class="input-group">
                            @Html.LabelFor(m => m.Email, new { @class = "control-label input-group-addon" })
                            @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
                            
                        </div>
                        @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })
                    </div>
                </div>
                <div class="form-group">
                    
                    <div class="col-md-10">
                        <div class="input-group">
                            @Html.LabelFor(m => m.Password, new { @class = "control-label input-group-addon" })
                            @Html.PasswordFor(m => m.Password, new { @class = "form-control" })
                            
                        </div>
                        @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger" })
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-md-10">
                           
                            <div class="input-group">

                                <span class="input-group-addon">@Html.CheckBoxFor(m => m.RememberMe)</span>
                                @Html.LabelFor(m => m.RememberMe, htmlAttributes: new { @class = "form-control" })

                            </div>


                    </div>
                </div>
                <div class="form-group">
                    <div class="col-md-10 text-right">
                        <input type="submit" value="Efetuar login" class="btn btn-primary" />
                    </div>
                </div>

            }
        </section>
    </div>
    <div class="col-md-4">
        <section id="socialLoginForm">
            @Html.Partial("_ExternalLoginsListPartial", new ExternalLoginListViewModel { ReturnUrl = ViewBag.ReturnUrl })
        </section>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}